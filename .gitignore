.DS_Store

# Data files — BigQuery is the persistence layer; no raw data in the repo
data/

# Credentials — never commit service account keys
secrets/
*-sa-key.json
*-credentials.json
service-account*.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# Claude local settings
.claude/settings.local.json
